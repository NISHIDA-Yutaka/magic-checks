import 'package:flutter/material.dart';

// Webでは何もしない空の関数を定義
void startNfcSession(BuildContext context) {
  // WebではNFCはサポートされていません
}

Future<void> linkNfcTag(BuildContext context, String checklistId, {required bool write}) async {
  // WebではNFCはサポートされていません
}
